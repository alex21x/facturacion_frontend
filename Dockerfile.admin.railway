FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --include=optional

COPY . .

RUN npm run build:admin

EXPOSE 3000

CMD ["sh", "-lc", "node scripts/serve-static.mjs dist-admin"]